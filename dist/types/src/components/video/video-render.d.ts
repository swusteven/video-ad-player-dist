import { VideoOptions } from './video';
import { VastInformation } from '../../services/vast-model';
export declare const renderVideo: (vastInformation: VastInformation, elementId: string, options: VideoOptions) => Promise<void>;
